<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.orbitworkbench.workflow.infrastructure.mapper.WorkflowRunEventMapper">

    <insert id="insert"
            parameterType="com.orbitworkbench.workflow.domain.WorkflowRunEventRecord"
            useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO workflow_run_event (
            workflow_run_id, sequence, event_type, event_summary,
            payload_json, occurred_at
        ) VALUES (
            #{workflowRunId}, #{sequence}, #{eventType}, #{eventSummary},
            #{payloadJson}, #{occurredAt}
        )
    </insert>

    <update id="incrementSequence">
        UPDATE workflow_run
        SET event_sequence = event_sequence + 1
        WHERE id = #{workflowRunId}
    </update>

    <select id="currentSequence" resultType="long">
        SELECT event_sequence
        FROM workflow_run
        WHERE id = #{workflowRunId}
    </select>

    <select id="findAfterSequence"
            resultType="com.orbitworkbench.workflow.domain.WorkflowRunEventRecord">
        SELECT id, workflow_run_id, sequence, event_type, event_summary,
               payload_json, occurred_at
        FROM workflow_run_event
        WHERE workflow_run_id = #{workflowRunId}
          AND sequence &gt; #{afterSequence}
        ORDER BY sequence
        LIMIT #{limit}
    </select>
</mapper>
