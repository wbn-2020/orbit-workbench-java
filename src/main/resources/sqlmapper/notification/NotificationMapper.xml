<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.orbitworkbench.notification.infrastructure.mapper.NotificationMapper">
    <resultMap id="NotificationMap" type="com.orbitworkbench.notification.domain.NotificationRecord">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="eventType" column="event_type"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="resourceType" column="resource_type"/>
        <result property="resourceId" column="resource_id"/>
        <result property="resourceRoute" column="resource_route"/>
        <result property="idempotencyKey" column="idempotency_key"/>
        <result property="readAt" column="read_at"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <sql id="allColumns">
        id, user_id, event_type, title, content, resource_type, resource_id,
        resource_route, idempotency_key, read_at, created_at
    </sql>

    <insert id="insertIgnore" parameterType="com.orbitworkbench.notification.domain.NotificationRecord">
        INSERT IGNORE INTO notification(
            user_id, event_type, title, content, resource_type, resource_id,
            resource_route, idempotency_key, read_at, created_at
        )
        VALUES(
            #{userId}, #{eventType}, #{title}, #{content}, #{resourceType}, #{resourceId},
            #{resourceRoute}, #{idempotencyKey}, #{readAt}, #{createdAt}
        )
    </insert>

    <select id="findByIdAndUser" resultMap="NotificationMap">
        SELECT <include refid="allColumns"/>
        FROM notification
        WHERE id = #{id} AND user_id = #{userId}
    </select>

    <select id="listByUser" resultMap="NotificationMap">
        SELECT <include refid="allColumns"/>
        FROM notification
        WHERE user_id = #{userId}
        <if test="unreadOnly">
            AND read_at IS NULL
        </if>
        ORDER BY (read_at IS NULL) DESC, created_at DESC, id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByUser" resultType="int">
        SELECT COUNT(*)
        FROM notification
        WHERE user_id = #{userId}
        <if test="unreadOnly">
            AND read_at IS NULL
        </if>
    </select>

    <update id="markRead">
        UPDATE notification
        SET read_at = #{readAt}
        WHERE id = #{id} AND user_id = #{userId} AND read_at IS NULL
    </update>

    <update id="markAllRead">
        UPDATE notification
        SET read_at = #{readAt}
        WHERE user_id = #{userId} AND read_at IS NULL
    </update>
</mapper>
