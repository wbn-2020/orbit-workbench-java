<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.orbitworkbench.memory.infrastructure.mapper.MemoryCandidateMapper">

    <resultMap id="CandidateResultMap"
               type="com.orbitworkbench.memory.domain.MemoryCandidateRecord">
        <id property="id" column="id"/>
        <result property="workspaceId" column="workspace_id"/>
        <result property="memoryType" column="memory_type"/>
        <result property="contentJson" column="content_json"/>
        <result property="sourceType" column="source_type"/>
        <result property="sourceId" column="source_id"/>
        <result property="confidence" column="confidence"/>
        <result property="expiresAt" column="expires_at"/>
        <result property="status" column="status"/>
        <result property="version" column="version"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="Columns">
        memory_candidate.id, memory_candidate.workspace_id, memory_candidate.memory_type,
        memory_candidate.content_json, memory_candidate.source_type, memory_candidate.source_id,
        memory_candidate.confidence, memory_candidate.expires_at, memory_candidate.status,
        memory_candidate.version, memory_candidate.created_at, memory_candidate.updated_at
    </sql>

    <select id="findPage" resultMap="CandidateResultMap">
        SELECT <include refid="Columns"/>
        FROM memory_candidate
        JOIN workspace w ON w.id = memory_candidate.workspace_id AND w.status = 'ACTIVE'
        <where>
            <if test="workspaceId != null">
                AND memory_candidate.workspace_id = #{workspaceId}
            </if>
            <if test="status != null and status != ''">
                AND memory_candidate.status = #{status}
            </if>
        </where>
        ORDER BY memory_candidate.updated_at DESC, memory_candidate.id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countPage" resultType="long">
        SELECT COUNT(*)
        FROM memory_candidate
        JOIN workspace w ON w.id = memory_candidate.workspace_id AND w.status = 'ACTIVE'
        <where>
            <if test="workspaceId != null">
                AND memory_candidate.workspace_id = #{workspaceId}
            </if>
            <if test="status != null and status != ''">
                AND memory_candidate.status = #{status}
            </if>
        </where>
    </select>

    <select id="findByIdForUpdate" resultMap="CandidateResultMap">
        SELECT <include refid="Columns"/>
        FROM memory_candidate
        JOIN workspace w ON w.id = memory_candidate.workspace_id AND w.status = 'ACTIVE'
        WHERE memory_candidate.id = #{id}
        FOR UPDATE
    </select>

    <insert id="insert"
            parameterType="com.orbitworkbench.memory.domain.MemoryCandidateRecord"
            useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO memory_candidate (
            workspace_id, memory_type, content_json, source_type, source_id,
            confidence, expires_at, status, version, created_at, updated_at
        ) VALUES (
            #{workspaceId}, #{memoryType}, #{contentJson}, #{sourceType}, #{sourceId},
            #{confidence}, #{expiresAt}, #{status}, #{version}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <update id="updateStatus">
        UPDATE memory_candidate
        SET status = #{status},
            version = version + 1,
            updated_at = #{now}
        WHERE id = #{id}
          AND status = #{expectedStatus}
          AND version = #{expectedVersion}
    </update>
</mapper>
