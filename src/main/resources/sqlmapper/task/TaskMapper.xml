<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.orbitworkbench.task.infrastructure.mapper.TaskMapper">

    <resultMap id="TaskResultMap" type="com.orbitworkbench.task.domain.TaskRecord">
        <id property="id" column="id"/>
        <result property="workspaceId" column="workspace_id"/>
        <result property="connectionId" column="connection_id"/>
        <result property="moduleType" column="module_type"/>
        <result property="title" column="title"/>
        <result property="description" column="description"/>
        <result property="expectedArtifactType" column="expected_artifact_type"/>
        <result property="priority" column="priority"/>
        <result property="status" column="status"/>
        <result property="currentRunId" column="current_run_id"/>
        <result property="currentRunStatus" column="current_run_status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="TaskColumns">
        id, workspace_id, connection_id, module_type, title, description,
        expected_artifact_type, priority, status, current_run_id,
        (SELECT ar.status FROM agent_run ar WHERE ar.id = task.current_run_id) AS current_run_status,
        created_at, updated_at
    </sql>

    <insert id="insert" parameterType="com.orbitworkbench.task.domain.TaskRecord"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO task (
            workspace_id, connection_id, module_type, title, description,
            expected_artifact_type, priority, status, current_run_id, created_at, updated_at
        ) VALUES (
            #{workspaceId}, #{connectionId}, #{moduleType}, #{title}, #{description},
            #{expectedArtifactType}, #{priority}, #{status}, #{currentRunId}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <insert id="insertCreateReservation">
        INSERT IGNORE INTO task_create_idempotency (
            idempotency_key, request_fingerprint, task_id, created_at, updated_at
        ) VALUES (
            #{idempotencyKey}, #{requestFingerprint}, NULL, #{now}, #{now}
        )
    </insert>

    <select id="findCreateReservationForUpdate"
            resultType="com.orbitworkbench.task.domain.TaskCreateIdempotencyRecord">
        SELECT idempotency_key, request_fingerprint, task_id
        FROM task_create_idempotency
        WHERE idempotency_key = #{idempotencyKey}
        FOR UPDATE
    </select>

    <update id="attachCreatedTask">
        UPDATE task_create_idempotency
        SET task_id = #{taskId},
            updated_at = #{now}
        WHERE idempotency_key = #{idempotencyKey}
          AND request_fingerprint = #{requestFingerprint}
          AND task_id IS NULL
    </update>

    <select id="findById" resultMap="TaskResultMap">
        SELECT <include refid="TaskColumns"/>
        FROM task
        WHERE id = #{id}
    </select>

    <select id="findByIdForUpdate" resultMap="TaskResultMap">
        SELECT <include refid="TaskColumns"/>
        FROM task
        WHERE id = #{id}
        FOR UPDATE
    </select>

    <select id="findPage" resultMap="TaskResultMap">
        SELECT <include refid="TaskColumns"/>
        FROM task
        <where>
            <if test="workspaceId != null">
                workspace_id = #{workspaceId}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
            <if test="moduleType != null">
                AND module_type = #{moduleType}
            </if>
        </where>
        ORDER BY updated_at DESC, id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countPage" resultType="long">
        SELECT COUNT(*)
        FROM task
        <where>
            <if test="workspaceId != null">
                workspace_id = #{workspaceId}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
            <if test="moduleType != null">
                AND module_type = #{moduleType}
            </if>
        </where>
    </select>

    <update id="updateEditable" parameterType="com.orbitworkbench.task.domain.TaskRecord">
        UPDATE task
        SET connection_id = #{connectionId},
            module_type = #{moduleType},
            title = #{title},
            description = #{description},
            expected_artifact_type = #{expectedArtifactType},
            priority = #{priority},
            updated_at = #{updatedAt}
        WHERE id = #{id}
          AND status IN ('DRAFT', 'READY', 'PAUSED')
    </update>

    <update id="updateRunState">
        UPDATE task
        SET current_run_id = #{currentRunId},
            status = #{status},
            updated_at = UTC_TIMESTAMP(6)
        WHERE id = #{id}
          AND status = #{fromStatus}
    </update>

    <update id="updateStatusForCurrentRun">
        UPDATE task
        SET status = #{status},
            updated_at = UTC_TIMESTAMP(6)
        WHERE id = #{id}
          AND current_run_id = #{currentRunId}
    </update>

    <select id="lockActiveWorkspace" resultType="long">
        SELECT id
        FROM workspace
        WHERE id = #{workspaceId}
          AND status = 'ACTIVE'
        FOR UPDATE
    </select>

    <select id="lockUsableConnection" resultType="long">
        SELECT id
        FROM ai_connection
        WHERE id = #{connectionId}
          AND enabled = 1
          AND deleted_at IS NULL
        FOR UPDATE
    </select>

    <select id="lockDocuments" resultType="long">
        SELECT id
        FROM document
        WHERE workspace_id = #{workspaceId}
          AND id IN
          <foreach collection="documentIds" item="documentId" open="(" separator="," close=")">
              #{documentId}
          </foreach>
        ORDER BY id
        FOR UPDATE
    </select>

    <insert id="insertTaskDocument">
        INSERT INTO task_document (task_id, document_id, relation_type, created_at)
        VALUES (#{taskId}, #{documentId}, #{relationType}, UTC_TIMESTAMP(6))
    </insert>

    <delete id="deleteTaskDocuments">
        DELETE FROM task_document WHERE task_id = #{taskId}
    </delete>

    <select id="findDocumentIds" resultType="long">
        SELECT document_id
        FROM task_document
        WHERE task_id = #{taskId}
        ORDER BY document_id
    </select>
</mapper>
