<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.orbitworkbench.tool.infrastructure.mapper.SkillVersionToolMapper">

    <select id="findToolVersionIds" resultType="long">
        SELECT tool_version_id
        FROM skill_version_tool
        WHERE skill_version_id = #{skillVersionId}
        ORDER BY binding_order
    </select>

    <insert id="insert"
            parameterType="com.orbitworkbench.tool.domain.SkillVersionToolBindingRecord">
        INSERT INTO skill_version_tool (
            skill_version_id, tool_version_id, binding_order, created_at
        ) VALUES (
            #{skillVersionId}, #{toolVersionId}, #{bindingOrder}, #{createdAt}
        )
    </insert>

    <delete id="deleteBySkillVersionId">
        DELETE FROM skill_version_tool
        WHERE skill_version_id = #{skillVersionId}
    </delete>
</mapper>
