<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.orbitworkbench.tool.infrastructure.mapper.ToolDefinitionMapper">

    <resultMap id="DefinitionResultMap"
               type="com.orbitworkbench.tool.domain.ToolDefinitionRecord">
        <id property="id" column="id"/>
        <result property="toolCode" column="tool_code"/>
        <result property="name" column="name"/>
        <result property="description" column="description"/>
        <result property="handlerType" column="handler_type"/>
        <result property="toolVersion" column="tool_version"/>
        <result property="inputSchemaJson" column="input_schema_json"/>
        <result property="outputSchemaJson" column="output_schema_json"/>
        <result property="riskLevel" column="risk_level"/>
        <result property="requiresConfirmation" column="requires_confirmation"/>
        <result property="timeoutMs" column="timeout_ms"/>
        <result property="maxResultBytes" column="max_result_bytes"/>
        <result property="enabled" column="enabled"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="DefinitionColumns">
        id, tool_code, name, description, handler_type, tool_version,
        input_schema_json, output_schema_json, risk_level,
        requires_confirmation, timeout_ms, max_result_bytes,
        enabled, created_at, updated_at
    </sql>

    <select id="findEnabledLatestByCode" resultMap="DefinitionResultMap">
        SELECT <include refid="DefinitionColumns"/>
        FROM tool_definition
        WHERE tool_code = #{toolCode}
          AND enabled = 1
        ORDER BY tool_version DESC, id DESC
        LIMIT 1
    </select>

    <select id="findEnabledByCodeAndVersion" resultMap="DefinitionResultMap">
        SELECT <include refid="DefinitionColumns"/>
        FROM tool_definition
        WHERE tool_code = #{toolCode}
          AND tool_version = #{toolVersion}
          AND enabled = 1
        LIMIT 1
    </select>

    <select id="findById" resultMap="DefinitionResultMap">
        SELECT <include refid="DefinitionColumns"/>
        FROM tool_definition
        WHERE id = #{id}
    </select>

    <select id="findAllEnabled" resultMap="DefinitionResultMap">
        SELECT <include refid="DefinitionColumns"/>
        FROM tool_definition
        WHERE enabled = 1
        ORDER BY tool_code, tool_version DESC, id DESC
    </select>
</mapper>
