<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.orbitworkbench.skill.infrastructure.mapper.SkillVersionMapper">

    <resultMap id="VersionResultMap"
               type="com.orbitworkbench.skill.domain.SkillVersionRecord">
        <id property="id" column="id"/>
        <result property="skillDefinitionId" column="skill_definition_id"/>
        <result property="versionNumber" column="version_number"/>
        <result property="status" column="status"/>
        <result property="promptVersionId" column="prompt_version_id"/>
        <result property="promptVersionNumber" column="prompt_version_number"/>
        <result property="inputSchemaJson" column="input_schema_json"/>
        <result property="outputType" column="output_type"/>
        <result property="runtimeLimitsJson" column="runtime_limits_json"/>
        <result property="publishedAt" column="published_at"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="Columns">
        sv.id, sv.skill_definition_id, sv.version_number, sv.status,
        sv.prompt_version_id, pv.version_number AS prompt_version_number,
        sv.input_schema_json, sv.output_type, sv.runtime_limits_json,
        sv.published_at, sv.created_at, sv.updated_at
    </sql>

    <insert id="insert"
            parameterType="com.orbitworkbench.skill.domain.SkillVersionRecord"
            useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO skill_version (
            skill_definition_id, version_number, status, prompt_version_id,
            input_schema_json, output_type, runtime_limits_json,
            published_at, created_at, updated_at
        ) VALUES (
            #{skillDefinitionId}, #{versionNumber}, #{status}, #{promptVersionId},
            #{inputSchemaJson}, #{outputType}, #{runtimeLimitsJson},
            #{publishedAt}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <select id="findById" resultMap="VersionResultMap">
        SELECT <include refid="Columns"/>
        FROM skill_version sv
        JOIN prompt_version pv ON pv.id = sv.prompt_version_id
        WHERE sv.id = #{id}
    </select>

    <select id="findByIdForUpdate" resultMap="VersionResultMap">
        SELECT <include refid="Columns"/>
        FROM skill_version sv
        JOIN prompt_version pv ON pv.id = sv.prompt_version_id
        WHERE sv.id = #{id}
        FOR UPDATE
    </select>

    <select id="findDraftForUpdate" resultMap="VersionResultMap">
        SELECT <include refid="Columns"/>
        FROM skill_version sv
        JOIN prompt_version pv ON pv.id = sv.prompt_version_id
        WHERE sv.skill_definition_id = #{skillDefinitionId}
          AND sv.status = 'DRAFT'
        ORDER BY sv.version_number DESC, sv.id DESC
        LIMIT 1
        FOR UPDATE
    </select>

    <select id="findBySkillDefinitionId" resultMap="VersionResultMap">
        SELECT <include refid="Columns"/>
        FROM skill_version sv
        JOIN prompt_version pv ON pv.id = sv.prompt_version_id
        WHERE sv.skill_definition_id = #{skillDefinitionId}
        ORDER BY sv.version_number DESC, sv.id DESC
    </select>

    <update id="updateDraft"
            parameterType="com.orbitworkbench.skill.domain.SkillVersionRecord">
        UPDATE skill_version
        SET prompt_version_id = #{promptVersionId},
            input_schema_json = #{inputSchemaJson},
            output_type = #{outputType},
            runtime_limits_json = #{runtimeLimitsJson},
            updated_at = #{updatedAt}
        WHERE id = #{id}
          AND skill_definition_id = #{skillDefinitionId}
          AND status = 'DRAFT'
    </update>

    <update id="publishDraft">
        UPDATE skill_version
        SET status = 'PUBLISHED',
            published_at = #{now},
            updated_at = #{now}
        WHERE id = #{versionId}
          AND skill_definition_id = #{skillDefinitionId}
          AND status = 'DRAFT'
    </update>

    <update id="disableAll">
        UPDATE skill_version
        SET status = 'DISABLED',
            updated_at = #{now}
        WHERE skill_definition_id = #{skillDefinitionId}
          AND status = 'DRAFT'
    </update>

    <update id="disableDrafts">
        UPDATE skill_version
        SET status = 'DISABLED',
            updated_at = #{now}
        WHERE skill_definition_id = #{skillDefinitionId}
          AND status = 'DRAFT'
    </update>

    <select id="nextVersionNumber" resultType="int">
        SELECT COALESCE(MAX(version_number), 0) + 1
        FROM skill_version
        WHERE skill_definition_id = #{skillDefinitionId}
    </select>
</mapper>
