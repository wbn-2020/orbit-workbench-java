<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.orbitworkbench.skill.infrastructure.mapper.SkillDefinitionMapper">

    <resultMap id="DefinitionResultMap"
               type="com.orbitworkbench.skill.domain.SkillDefinitionRecord">
        <id property="id" column="id"/>
        <result property="workspaceId" column="workspace_id"/>
        <result property="skillCode" column="skill_code"/>
        <result property="name" column="name"/>
        <result property="description" column="description"/>
        <result property="publishedVersionId" column="published_version_id"/>
        <result property="status" column="status"/>
        <result property="lockVersion" column="lock_version"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="Columns">
        id, workspace_id, skill_code, name, description,
        published_version_id, status, lock_version, created_at, updated_at
    </sql>

    <select id="findByWorkspaceId" resultMap="DefinitionResultMap">
        SELECT <include refid="Columns"/>
        FROM skill_definition
        WHERE workspace_id = #{workspaceId}
        ORDER BY updated_at DESC, id DESC
    </select>

    <select id="findById" resultMap="DefinitionResultMap">
        SELECT <include refid="Columns"/>
        FROM skill_definition
        WHERE id = #{id}
    </select>

    <select id="findByIdForUpdate" resultMap="DefinitionResultMap">
        SELECT <include refid="Columns"/>
        FROM skill_definition
        WHERE id = #{id}
        FOR UPDATE
    </select>

    <select id="findByWorkspaceCode" resultMap="DefinitionResultMap">
        SELECT <include refid="Columns"/>
        FROM skill_definition
        WHERE workspace_id = #{workspaceId}
          AND skill_code = #{skillCode}
        LIMIT 1
    </select>

    <insert id="insert"
            parameterType="com.orbitworkbench.skill.domain.SkillDefinitionRecord"
            useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO skill_definition (
            workspace_id, skill_code, name, description,
            published_version_id, status, lock_version, created_at, updated_at
        ) VALUES (
            #{workspaceId}, #{skillCode}, #{name}, #{description},
            #{publishedVersionId}, #{status}, #{lockVersion}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <update id="update"
            parameterType="com.orbitworkbench.skill.domain.SkillDefinitionRecord">
        UPDATE skill_definition
        SET skill_code = #{definition.skillCode},
            name = #{definition.name},
            description = #{definition.description},
            lock_version = lock_version + 1,
            updated_at = #{definition.updatedAt}
        WHERE id = #{definition.id}
          AND lock_version = #{expectedLockVersion}
          AND status != 'DISABLED'
    </update>

    <update id="bumpLockVersion">
        UPDATE skill_definition
        SET lock_version = lock_version + 1,
            updated_at = #{now}
        WHERE id = #{id}
          AND lock_version = #{expectedLockVersion}
          AND status != 'DISABLED'
    </update>

    <update id="setPublishedVersion">
        UPDATE skill_definition
        SET published_version_id = #{versionId},
            status = 'PUBLISHED',
            lock_version = lock_version + 1,
            updated_at = #{now}
        WHERE id = #{id}
          AND status != 'DISABLED'
    </update>

    <update id="disable">
        UPDATE skill_definition
        SET status = 'DISABLED',
            lock_version = lock_version + 1,
            updated_at = #{now}
        WHERE id = #{id}
          AND status != 'DISABLED'
    </update>
</mapper>
