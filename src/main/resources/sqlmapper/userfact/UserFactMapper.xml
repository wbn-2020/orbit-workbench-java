<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.orbitworkbench.userfact.infrastructure.mapper.UserFactMapper">
    <resultMap id="UserFactMap" type="com.orbitworkbench.userfact.domain.UserFactRecord">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="factType" column="fact_type"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="source" column="source"/>
        <result property="confirmationStatus" column="confirmation_status"/>
        <result property="confidence" column="confidence"/>
        <result property="confirmedAt" column="confirmed_at"/>
        <result property="lastSeenAt" column="last_seen_at"/>
        <result property="archivedReason" column="archived_reason"/>
        <result property="sourceHint" column="source_hint"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="allColumns">
        id, user_id, fact_type, title, content, source, confirmation_status,
        confidence, confirmed_at, last_seen_at, archived_reason, source_hint,
        created_at, updated_at
    </sql>

    <insert id="insert" parameterType="com.orbitworkbench.userfact.domain.UserFactRecord"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_fact(
            user_id, fact_type, title, content, source, confirmation_status,
            confidence, confirmed_at, last_seen_at, archived_reason, source_hint,
            created_at, updated_at
        )
        VALUES(
            #{userId}, #{factType}, #{title}, #{content}, #{source}, #{confirmationStatus},
            #{confidence}, #{confirmedAt}, #{lastSeenAt}, #{archivedReason}, #{sourceHint},
            #{createdAt}, #{updatedAt}
        )
    </insert>

    <select id="listByUser" resultMap="UserFactMap">
        SELECT <include refid="allColumns"/>
        FROM user_fact
        WHERE user_id = #{userId}
        ORDER BY
            CASE confirmation_status WHEN 'CONFIRMED' THEN 0
                                     WHEN 'ANALYZED' THEN 1
                                     ELSE 2 END,
            id DESC
    </select>

    <select id="findByIdAndUser" resultMap="UserFactMap">
        SELECT <include refid="allColumns"/>
        FROM user_fact
        WHERE id = #{id} AND user_id = #{userId}
    </select>

    <select id="listConfirmed" resultMap="UserFactMap">
        SELECT <include refid="allColumns"/>
        FROM user_fact
        WHERE user_id = #{userId} AND confirmation_status = 'CONFIRMED'
        ORDER BY COALESCE(last_seen_at, confirmed_at, updated_at) DESC, id DESC
        LIMIT #{limit}
    </select>

    <update id="confirm">
        UPDATE user_fact
        SET fact_type = #{factType},
            title = #{title},
            content = #{content},
            confirmation_status = 'CONFIRMED',
            confirmed_at = #{confirmedAt},
            last_seen_at = #{confirmedAt},
            updated_at = #{updatedAt}
        WHERE id = #{id}
          AND user_id = #{userId}
          AND confirmation_status = 'ANALYZED'
    </update>

    <update id="archive">
        UPDATE user_fact
        SET confirmation_status = 'ARCHIVED',
            archived_reason = #{reason},
            updated_at = #{updatedAt}
        WHERE id = #{id}
          AND user_id = #{userId}
          AND confirmation_status != 'ARCHIVED'
    </update>

    <!-- 复查：用户确认这条事实「仍然成立」，把 last_seen_at 推到当下。
         只对 CONFIRMED 生效——候选池与已归档没有「时效」可言。 -->
    <update id="reaffirm">
        UPDATE user_fact
        SET last_seen_at = #{seenAt},
            updated_at = #{updatedAt}
        WHERE id = #{id}
          AND user_id = #{userId}
          AND confirmation_status = 'CONFIRMED'
    </update>

    <select id="countAnalyzed" resultType="long">
        SELECT COUNT(*)
        FROM user_fact
        WHERE user_id = #{userId} AND confirmation_status = 'ANALYZED'
    </select>

    <select id="listConfirmedForPrompt" resultMap="UserFactMap">
        SELECT <include refid="allColumns"/>
        FROM user_fact
        WHERE user_id = #{userId} AND confirmation_status = 'CONFIRMED'
        ORDER BY fact_type ASC, id ASC
        LIMIT 60
    </select>
</mapper>
