<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.orbitworkbench.learning.infrastructure.mapper.LearningGoalMapper">
    <resultMap id="LearningGoalRowMap" type="com.orbitworkbench.learning.domain.LearningGoalRow">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="reason" column="reason"/>
        <result property="status" column="status"/>
        <result property="progress" column="progress"/>
        <result property="linkedSkill" column="linked_skill"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.orbitworkbench.learning.domain.LearningGoalRecord"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO learning_goal(
            user_id, title, reason, status, progress, linked_skill, idempotency_key, version, created_at, updated_at
        )
        VALUES(
            #{userId}, #{title}, #{reason}, #{status}, #{progress}, #{linkedSkill}, #{idempotencyKey}, #{version}, #{createdAt}, #{updatedAt}
        )
        ON DUPLICATE KEY UPDATE id = LAST_INSERT_ID(id)
    </insert>

    <select id="findOwned" resultMap="LearningGoalRowMap">
        SELECT id, title, reason, status, progress, linked_skill, created_at
        FROM learning_goal
        WHERE user_id = #{userId} AND id = #{id}
    </select>

    <select id="findByIdempotencyKey" resultMap="LearningGoalRowMap">
        SELECT id, title, reason, status, progress, linked_skill, created_at
        FROM learning_goal
        WHERE user_id = #{userId} AND idempotency_key = #{idempotencyKey}
    </select>

    <select id="listByUser" resultMap="LearningGoalRowMap">
        SELECT id, title, reason, status, progress, linked_skill, created_at
        FROM learning_goal
        WHERE user_id = #{userId}
        ORDER BY
            CASE status WHEN 'ACTIVE' THEN 0 WHEN 'PAUSED' THEN 1 ELSE 2 END,
            updated_at DESC, id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <update id="updateStatusAndProgress">
        UPDATE learning_goal
        SET status = #{status}, progress = #{progress}, updated_at = #{updatedAt}
        WHERE id = #{id} AND user_id = #{userId}
    </update>
</mapper>
