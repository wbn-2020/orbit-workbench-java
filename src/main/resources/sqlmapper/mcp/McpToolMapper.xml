<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.orbitworkbench.mcp.infrastructure.mapper.McpToolMapper">

    <resultMap id="ToolResultMap"
               type="com.orbitworkbench.mcp.domain.McpToolRecord">
        <id property="id" column="id"/>
        <result property="mcpServerId" column="mcp_server_id"/>
        <result property="toolCode" column="tool_code"/>
        <result property="toolName" column="tool_name"/>
        <result property="title" column="title"/>
        <result property="description" column="description"/>
        <result property="toolCatalogId" column="tool_catalog_id"/>
        <result property="toolVersionId" column="tool_version_id"/>
        <result property="versionNumber" column="version_number"/>
        <result property="enabled" column="enabled"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="Columns">
        id, mcp_server_id, tool_code, tool_name, title, description,
        tool_catalog_id, tool_version_id, version_number, enabled,
        created_at, updated_at
    </sql>

    <select id="findByServerId" resultMap="ToolResultMap">
        SELECT <include refid="Columns"/>
        FROM mcp_tool
        WHERE mcp_server_id = #{serverId}
        ORDER BY tool_name, id
    </select>

    <select id="findById" resultMap="ToolResultMap">
        SELECT <include refid="Columns"/>
        FROM mcp_tool
        WHERE id = #{id}
    </select>

    <select id="findByToolCode" resultMap="ToolResultMap">
        SELECT <include refid="Columns"/>
        FROM mcp_tool
        WHERE tool_code = #{toolCode}
    </select>

    <select id="findByServerAndName" resultMap="ToolResultMap">
        SELECT <include refid="Columns"/>
        FROM mcp_tool
        WHERE mcp_server_id = #{serverId}
          AND tool_name = #{toolName}
    </select>

    <insert id="insert"
            parameterType="com.orbitworkbench.mcp.domain.McpToolRecord"
            useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO mcp_tool (
            mcp_server_id, tool_code, tool_name, title, description,
            tool_catalog_id, tool_version_id, version_number, enabled,
            created_at, updated_at
        ) VALUES (
            #{mcpServerId}, #{toolCode}, #{toolName}, #{title}, #{description},
            #{toolCatalogId}, #{toolVersionId}, #{versionNumber}, #{enabled},
            #{createdAt}, #{updatedAt}
        )
    </insert>

    <update id="update"
            parameterType="com.orbitworkbench.mcp.domain.McpToolRecord">
        UPDATE mcp_tool
        SET title = #{title},
            description = #{description},
            tool_catalog_id = #{toolCatalogId},
            tool_version_id = #{toolVersionId},
            version_number = #{versionNumber},
            updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <update id="updateEnabled">
        UPDATE mcp_tool
        SET enabled = #{enabled},
            updated_at = #{now}
        WHERE id = #{id}
    </update>

    <update id="disableExceptNames">
        UPDATE mcp_tool
        SET enabled = 0,
            updated_at = #{now}
        WHERE mcp_server_id = #{serverId}
        <if test="toolNames != null and toolNames.size() > 0">
            AND tool_name NOT IN
            <foreach collection="toolNames" item="toolName"
                     open="(" separator="," close=")">
                #{toolName}
            </foreach>
        </if>
    </update>
</mapper>
