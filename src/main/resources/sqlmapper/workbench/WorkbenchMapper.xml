<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.orbitworkbench.workbench.infrastructure.mapper.WorkbenchMapper">
    <resultMap id="EvalMap" type="com.orbitworkbench.workbench.domain.WorkbenchLastEvaluation">
        <result property="hiringRecommendation" column="hiring_recommendation"/>
        <result property="totalScore" column="total_score"/>
    </resultMap>

    <select id="countWorkLogs" resultType="long">
        SELECT COUNT(*) FROM work_log WHERE user_id = #{userId}
    </select>

    <select id="countPendingDistill" resultType="long">
        SELECT COUNT(*) FROM work_log WHERE user_id = #{userId} AND distilled = 0
    </select>

    <select id="countKnowledgeCards" resultType="long">
        SELECT COUNT(*) FROM knowledge_card WHERE user_id = #{userId}
    </select>

    <select id="countLearningGoals" resultType="long">
        SELECT COUNT(*) FROM learning_goal WHERE user_id = #{userId}
    </select>

    <select id="countActiveGoals" resultType="long">
        SELECT COUNT(*) FROM learning_goal WHERE user_id = #{userId} AND status = 'ACTIVE'
    </select>

    <select id="sumFocusMinutesAll" resultType="long">
        SELECT COALESCE(SUM(duration_minutes), 0)
        FROM focus_session
        WHERE user_id = #{userId} AND mode = 'FOCUS'
    </select>

    <select id="focusMinutesToday" resultType="long">
        SELECT COALESCE(SUM(duration_minutes), 0)
        FROM focus_session
        WHERE user_id = #{userId} AND mode = 'FOCUS'
          AND started_at &gt;= #{start} AND started_at &lt; #{end}
    </select>

    <!-- 以报告插入序（id）而非 generated_at 取「最近一次评估」：重试会改写 generated_at，
         但用户关心的是最新一场面试的结论，插入序才是场次顺序。 -->
    <select id="lastInterviewGrade" resultMap="EvalMap">
        SELECT r.hiring_recommendation, r.total_score
        FROM interview_report r
        JOIN interview_session s ON s.id = r.session_id
        WHERE s.user_id = #{userId}
        ORDER BY r.id DESC
        LIMIT 1
    </select>

    <!-- 管线体检只读统计。ai_connection 是全局资产（V2 无 user_id 列），按启用数计。 -->
    <select id="countEnabledConnections" resultType="long">
        SELECT COUNT(*) FROM ai_connection WHERE enabled = 1
    </select>

    <select id="countProjects" resultType="long">
        SELECT COUNT(*) FROM project WHERE user_id = #{userId}
    </select>

    <select id="countReadyReports" resultType="long">
        SELECT COUNT(*)
        FROM interview_report r
        JOIN interview_session s ON s.id = r.session_id
        WHERE s.user_id = #{userId} AND r.status = 'REPORT_READY'
    </select>

    <select id="countFailedReports" resultType="long">
        SELECT COUNT(*)
        FROM interview_report r
        JOIN interview_session s ON s.id = r.session_id
        WHERE s.user_id = #{userId} AND r.status = 'REPORT_FAILED'
    </select>

    <select id="lastInterviewEndedAt" resultType="java.time.Instant">
        SELECT MAX(ended_at)
        FROM interview_session
        WHERE user_id = #{userId} AND status = 'COMPLETED'
    </select>

    <select id="countDueCards" resultType="long">
        SELECT COUNT(*)
        FROM knowledge_card
        WHERE user_id = #{userId}
          AND next_review_date IS NOT NULL AND next_review_date &lt;= #{date}
    </select>

    <select id="countPendingUserFacts" resultType="long">
        SELECT COUNT(*)
        FROM user_fact
        WHERE user_id = #{userId} AND confirmation_status = 'ANALYZED'
    </select>

    <!-- V49：已确认套路里，还没有 CRAFT 来源练习任务的那些。
         V50 补充：practice_count = 0——练熟的套路不再催促（即使练习任务后来被删） -->
    <select id="countUnpracticedCrafts" resultType="long">
        SELECT COUNT(*)
        FROM craft_note c
        WHERE c.user_id = #{userId}
          AND c.confirmation_status = 'CONFIRMED'
          AND c.practice_count = 0
          AND NOT EXISTS (
              SELECT 1 FROM study_task t
              WHERE t.user_id = c.user_id
                AND t.source_type = 'CRAFT'
                AND t.source_id = c.id
          )
    </select>
</mapper>
