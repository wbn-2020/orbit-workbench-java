<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.orbitworkbench.agent.infrastructure.mapper.AgentRunMapper">

    <resultMap id="AgentRunResultMap" type="com.orbitworkbench.agent.domain.AgentRunRecord">
        <id property="id" column="id"/>
        <result property="taskId" column="task_id"/>
        <result property="agentDefinitionId" column="agent_definition_id"/>
        <result property="connectionId" column="connection_id"/>
        <result property="status" column="status"/>
        <result property="currentStep" column="current_step"/>
        <result property="startedAt" column="started_at"/>
        <result property="finishedAt" column="finished_at"/>
        <result property="lastHeartbeatAt" column="last_heartbeat_at"/>
        <result property="cancelRequestedAt" column="cancel_requested_at"/>
        <result property="retryOfRunId" column="retry_of_run_id"/>
        <result property="errorCode" column="error_code"/>
        <result property="errorSummary" column="error_summary"/>
        <result property="traceId" column="trace_id"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="AgentRunColumns">
        id, task_id, agent_definition_id, connection_id, status, current_step, started_at,
        finished_at, last_heartbeat_at, cancel_requested_at, retry_of_run_id,
        error_code, error_summary, trace_id, created_at, updated_at
    </sql>

    <insert id="insert" parameterType="com.orbitworkbench.agent.domain.AgentRunRecord"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO agent_run (
            task_id, agent_definition_id, connection_id, status, current_step, started_at,
            finished_at, last_heartbeat_at, cancel_requested_at, retry_of_run_id,
            error_code, error_summary, trace_id, created_at, updated_at
        ) VALUES (
            #{taskId}, #{agentDefinitionId}, #{connectionId}, #{status}, #{currentStep}, #{startedAt},
            #{finishedAt}, #{lastHeartbeatAt}, #{cancelRequestedAt}, #{retryOfRunId},
            #{errorCode}, #{errorSummary}, #{traceId}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <select id="findById" resultMap="AgentRunResultMap">
        SELECT <include refid="AgentRunColumns"/>
        FROM agent_run
        WHERE id = #{id}
    </select>

    <select id="findByIdForUpdate" resultMap="AgentRunResultMap">
        SELECT <include refid="AgentRunColumns"/>
        FROM agent_run
        WHERE id = #{id}
        FOR UPDATE
    </select>

    <select id="findByTaskId" resultMap="AgentRunResultMap">
        SELECT <include refid="AgentRunColumns"/>
        FROM agent_run
        WHERE task_id = #{taskId}
        ORDER BY created_at DESC, id DESC
        LIMIT 100
    </select>

    <update id="markRunning">
        UPDATE agent_run
        SET status = 'RUNNING',
            started_at = COALESCE(started_at, #{now}),
            last_heartbeat_at = #{now},
            updated_at = #{now}
        WHERE id = #{id}
          AND status = 'QUEUED'
    </update>

    <update id="markRunningFromPaused">
        UPDATE agent_run
        SET status = 'RUNNING',
            started_at = COALESCE(started_at, #{now}),
            last_heartbeat_at = #{now},
            updated_at = #{now}
        WHERE id = #{id}
          AND status = 'PAUSED'
    </update>

    <update id="touchHeartbeat">
        UPDATE agent_run
        SET last_heartbeat_at = #{now},
            updated_at = #{now}
        WHERE id = #{id}
          AND status = 'RUNNING'
    </update>

    <update id="updateStatus">
        UPDATE agent_run
        SET status = #{toStatus},
            error_code = #{errorCode},
            error_summary = #{errorSummary},
            finished_at = #{finishedAt},
            updated_at = UTC_TIMESTAMP(6)
        WHERE id = #{id}
          AND status = #{fromStatus}
    </update>

    <update id="requestCancel">
        UPDATE agent_run
        SET status = CASE WHEN status = 'QUEUED' THEN 'CANCELLED' ELSE 'CANCELLING' END,
            cancel_requested_at = UTC_TIMESTAMP(6),
            updated_at = UTC_TIMESTAMP(6)
        WHERE id = #{id}
          AND status IN
          <foreach collection="fromStatuses" item="status" open="(" separator="," close=")">
              #{status}
          </foreach>
    </update>

    <select id="findInterruptedIds" resultType="long">
        SELECT id
        FROM agent_run
        WHERE status IN ('QUEUED', 'RUNNING', 'CANCELLING', 'PAUSING')
        ORDER BY id
    </select>

    <update id="markRecoveryRequired">
        UPDATE agent_run
        SET status = 'RECOVERY_REQUIRED',
            error_code = 'UPSTREAM_UNAVAILABLE',
            error_summary = '服务重启后运行未完成，需要恢复或重试',
            finished_at = UTC_TIMESTAMP(6),
            updated_at = UTC_TIMESTAMP(6)
        WHERE id = #{id}
          AND status IN ('QUEUED', 'RUNNING', 'CANCELLING', 'PAUSING')
    </update>

    <select id="findLatestRetryByRunId" resultMap="AgentRunResultMap">
        SELECT <include refid="AgentRunColumns"/>
        FROM agent_run
        WHERE retry_of_run_id = #{runId}
        ORDER BY created_at DESC, id DESC
        LIMIT 1
    </select>
</mapper>
